<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="/var/www/html/bootstrap.php"
         colors="true"
         stopOnFailure="false"
         cacheDirectory=".phpunit.cache"
         requireCoverageMetadata="false">

    <!-- Test Suites -->
    <testsuites>
        <testsuite name="Unit">
            <directory>/var/www/html/tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>/var/www/html/tests/Integration</directory>
        </testsuite>
    </testsuites>

    <!-- Code Coverage Configuration -->
    <source>
        <include>
            <directory suffix=".php">/var/www/html</directory>
        </include>
        <exclude>
            <!-- Exclure vendor et tests -->
            <directory>/var/www/html/vendor</directory>
            <directory>/var/www/html/tests</directory>
            <!-- Exclure bootstrap (pas de logique métier) -->
            <file>/var/www/html/bootstrap.php</file>
            <!-- Exclure index.php (point d'entrée, testé via integration) -->
            <file>/var/www/html/index.php</file>
        </exclude>
    </source>

    <!-- Coverage Reporting -->
    <coverage>
        <report>
            <html outputDirectory="/var/www/html/coverage/html"/>
            <text outputFile="php://stdout" showOnlySummary="false"/>
            <clover outputFile="/var/www/html/coverage/clover.xml"/>
            <cobertura outputFile="/var/www/html/coverage/cobertura.xml"/>
        </report>
    </coverage>

    <!-- Logging -->
    <logging>
        <junit outputFile="/var/www/html/coverage/junit.xml"/>
    </logging>

    <!-- PHP Settings -->
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="DEBUG" value="0"/>

        <!-- Mock environment variables for tests -->
        <env name="GCP_PROJECT_ID" value="test-project"/>
        <env name="GCP_PROCESSOR_ID" value="test-processor"/>
        <env name="GCP_LOCATION" value="eu"/>
        <env name="GOOGLE_OAUTH_CLIENT_ID" value="test-client-id.apps.googleusercontent.com"/>
        <env name="SPREADSHEET_ID" value="test-spreadsheet-id"/>
        <env name="ALLOWED_EMAILS" value="test@example.com"/>
        <env name="DEFAULT_SHEET" value="TestSheet"/>
        <env name="WHO_COLUMNS" value='{"Test":["A","B","C"]}'/>
    </php>
</phpunit>
