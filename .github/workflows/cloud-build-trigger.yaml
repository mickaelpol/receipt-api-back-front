# GitHub Actions Workflow - Info only
# Le déploiement se fait via Cloud Build triggers configurés sur GCP

name: Cloud Build Info

on:
  push:
    branches:
      - staging
      - main
  workflow_dispatch:

env:
  PROJECT_ID: '264113083582'
  REGION: 'europe-west9'
  SERVICE_NAME: 'receipt-parser'

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Info message
        run: |
          echo "ℹ️ Le déploiement se fait automatiquement via Cloud Build triggers configurés sur GCP."
          echo "📋 Configuration:"
          echo "   - Projet: ${{ env.PROJECT_ID }}"
          echo "   - Région: ${{ env.REGION }}"
          echo "   - Service: ${{ env.SERVICE_NAME }}"
          echo "   - Branche: ${{ github.ref_name }}"
          echo ""
          echo "🔗 Vérifiez le statut du build sur:"
          echo "   https://console.cloud.google.com/cloud-build/builds?project=${{ env.PROJECT_ID }}"
