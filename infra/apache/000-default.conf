<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/html/frontend

    # Alias pour l'API backend
    Alias /api /var/www/html/index.php

    # Configuration pour le frontend (document root)
    <Directory /var/www/html/frontend>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        
        # Configuration pour les fichiers PHP
        <FilesMatch \.php$>
            SetHandler application/x-httpd-php
        </FilesMatch>
        
        # Cache headers pour les icônes et favicons
        <FilesMatch "\.(ico|svg|png|jpg|jpeg|gif|webp)$">
            Header set Cache-Control "public, max-age=31536000, immutable"
            Header set Expires "Thu, 31 Dec 2037 23:55:55 GMT"
        </FilesMatch>
        
        # Cache headers pour le manifest
        <FilesMatch "manifest\.json$">
            Header set Cache-Control "public, max-age=86400"
            Header set Content-Type "application/manifest+json"
        </FilesMatch>
    </Directory>

    # Configuration pour l'API backend
    <Directory /var/www/html>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
        
        # Configuration pour les fichiers PHP
        <FilesMatch \.php$>
            SetHandler application/x-httpd-php
        </FilesMatch>
    </Directory>

    # Logs
    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
